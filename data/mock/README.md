# Placeholder Mock Data

These files are placeholder data for the SafeRoute benchmark layer only. They are not official shelter, hospital, road, rainfall, or civic datasets.

The backend imports `data/velachery/*` when present, otherwise it falls back to these mock files.

